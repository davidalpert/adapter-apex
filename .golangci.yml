linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: logur.dev/adapter/apex

linters:
    enable-all: true
    disable:
        - funlen
        - gomnd
        - testpackage
